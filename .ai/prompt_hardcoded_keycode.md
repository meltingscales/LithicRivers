Search for `keycode::` in our codebase to track down any hardcoded keycodes.

Print out the keycode:: values that you find.

We should prefer using `app.ui.keybinds.matches`. Next, I'll give you directions on what to do with the keycodes you find.
