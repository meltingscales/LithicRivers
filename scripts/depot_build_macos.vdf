// macOS depot build configuration
// Replace DepotID with your real macOS depot ID

"DepotBuild"
{
    "DepotID"       "YOUR_DEPOT_ID_MACOS"

    // Content root is inherited from AppBuild's ContentRoot (.)
    // Map all files in artifacts/ to the depot root
    "FileMapping"
    {
        "LocalPath"   "artifacts/*"
        "DepotPath"   "."
        "Recursive"   "1"
    }
}
